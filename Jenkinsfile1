pipeline {
 agent {
  label 'jenkins'
 }
   stages {
    stage ('BUILD') {
      steps {
       echo "this is new Build stage"
      }
    }

    stages {
    stage ('DEPLOY') {
      steps {
       echo "this is new Build stage"
      }
    }

    stages {
    stage ('TEST') {
      steps {
       echo "this is new Build stage"
      }
    }
   }

}
